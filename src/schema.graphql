
type Todo {
    id: ID! 
    title: String!
    done: Boolean!
}

type Query {
    hello: String!
    todos: [Todo!]!
}

type Mutation {
    addTodo(title: String!): Todo!
    toggleTodo(id: ID!): Todo!
}


